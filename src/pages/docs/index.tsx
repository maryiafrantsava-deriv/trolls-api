const Docs: React.FC = () => { 
  return (
    <div>
      This is Docs page
    </div>
  );
};


export default Docs;

// this page is hidden for now under the Header, that's why we need to add
// global styles for our app to distrubute header, page & footer 